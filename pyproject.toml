[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "llm-emotion-threshold-project"
version = "0.1.1"
description = "Reproducible LLM emotion-threshold experiments and configurable SAE pipelines"
requires-python = ">=3.10"

[tool.setuptools]
packages = ["project_core"]
